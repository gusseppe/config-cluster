# Registry where the source image is located
SOURCE_REGISTRY=docker.io/gusseppe

# Registry where the image will be pushed
TARGET_REGISTRY=vcf-np-w2-harbor-az1.sunat.peru/agentes

# Image name and tag for the source
SOURCE_IMAGE=agent-service:latest

# Image name and tag for the target
TARGET_IMAGE=agent-service:latest
